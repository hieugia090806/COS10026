<?php
    //Ouput the value of pi
    echo(pi(). "\n");
    //Fing the min numvber among the list
    echo(min(0, 150, 30, 20, -8, -200). "\n");
    //Find the max number among the list
    echo(max(0, 150, 30, 20, -8, -200). "\n");
    //Makes from negative to positive
    echo(abs(-6.7). "\n");
    //Find the squares of the number
    echo(sqrt(64). "\n");
    //Find the round of the number
    echo(round(0.60). "\n"); //if bigger than or equal to 0.5 then it will round up
    echo(round(0.4999999999). "\n"); // if smaller than 0.5 then it will round down
    //Find the random number
    echo(rand());
?>